{
  "name": "humanitai-site",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i ./src/input.css -o ./styles/tailwind.css --minify",
    "watch:css": "npx @tailwindcss/cli -i ./src/input.css -o ./styles/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^4.0.0",
    "@tailwindcss/cli": "^4.0.0"
  }
}
